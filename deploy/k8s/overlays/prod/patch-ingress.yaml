apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: minisocial-ingress
spec:
  rules:
    - host: app.minisocial.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend
                port:
                  number: 80
